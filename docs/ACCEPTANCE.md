# 验收记录

更新日期：2026-09-09。工程与模型质量分层记录；不存在“完整百万字小说已经验收通过”的结论。

## 已完成的工程验证

- TypeScript 与模块依赖方向检查通过。
- 40 项单元/集成测试通过：原子提交、幂等、租约、版本冲突、上下文预算、证据校验、取消、检查点恢复、导入、摘要谱系、隐私字段投影、SQLite 恢复、SSE 关闭等。
- 3 项浏览器端到端测试通过：建书与人物卡、正文保存/恢复/导出、世界规则、队列暂停、移动布局、蓝图编辑/确认及秘密字段。
- 生产前端构建通过。界面包、编辑器、关系图及依赖已拆包。
- 本地网页服务与 Worker 已启动，主模型和 Embedding 可连接。

运行日志：`.novel/evidence/check.log`、`tests.log`、`build.log`、`e2e.log`。截图为浏览器测试数据，位于 `.novel/evidence/workspace-desktop.png` 和 `workspace-mobile.png`。

## 百万字容量

最新报告：`.novel/evaluations/scale-1788883557240/report.json`。

400 章，1,230,073 个正文字符；SQLite 一致性备份恢复后字符数相同。中文全文检索 p95 约 3.22 ms，JSON 导出约 8.42 MB。数据为重复合成夹具，不能用这个结果证明生成文学质量、复杂检索质量或长篇模型连续性。

## 模型冒烟测试

报告：`.novel/evaluations/model-1788882513655.json`。5/5 通过：结构化输出、中文正文与思考隔离、单个道具冲突样例、真实取消、中文 Embedding 排序。

这组短测没有覆盖自动全书规划、长篇人物发展或所有任务提示词。更详细的本机资源与模板说明见 [模型联调](MODEL_NOTES.md)。

## 连续写作：保留所有失败与修订依据

| 提示词版本 | 记录                       | 实际结果                                                                                                  |
| ---------- | -------------------------- | --------------------------------------------------------------------------------------------------------- |
| 1.0        | `longform-1788882546911`   | 0 章收录；审校引用不在正文，正确停止                                                                      |
| 1.1        | `longform-1788883015349`   | 0 章收录；误报与整章改写导致修订预算耗尽                                                                  |
| 1.2        | `longform-1788883465437`   | 2 章收录后主动暂停；Codex 内容抽查发现第 2 章的主角知识越界，同模型审校漏检                               |
| 1.3        | `longform-1788884234278`   | 0 章收录后主动暂停；草稿违反钥匙开篇归属，审校漏检，未让错误进入正式稿                                    |
| 1.4        | `continuity-1788884900942` | 3/3 回归通过：阻断唯一道具冲突、阻断秘密无源获知、不误报合理模糊时间                                      |
| 1.4        | `longform-1788885287301`   | 3/10 章收录，共 1,203 字；第 4 章三次记忆引用校验失败后停止；另有首章自动修订范围扩张漏检，质量验收不通过 |

这些原始数据库、报告、草稿和正文均保留在 `.novel/evaluations/`。1.2 的失败案例是主角直接说出“名册曾被调走过”，此前正文没有建立他获知此事的来源，而该信息存在于另一人物的秘密设定。

连续测试采用独立原创夹具，固定已确认蓝图，目标 10 个约 600 字短章。它不是从零规划质量评测，也不是标准长章或完整百万字验收。从 1.3 起代码指纹和有效配置随每轮保存。

1.4 的 `content-audit.json` 保存了 Codex 对首章修订范围的抽查：原稿没有字条，自动修订新增“铜钥匙在林舟手里”的字条来解释信息来源，后续章节沿用。这个抽查独立于被测本地模型，但不冒充独立人工文学评审。精确原文替换约束拒绝模糊锚点和重叠替换，仍不能保证语义上的最小修改范围。连续脚本的 `passed` 仅表示预定章数成功收录；它不覆盖这项内容审查，也不能推翻其失败结论。

最新运行耗时约 43.8 分钟，包含共享模型排队。第 4 章提取引用反复遗漏原文中的结束引号，三次输出仍未通过逐字定位，任务以 `failed` 结束，未收录该章或其记忆。失败报告、草稿检查点、每次重试和上下文均保留；没有删除失败、放宽匹配规则或把未完成运行算作通过。

## 尚未验收

- 实际百万字完整生成、跨卷连续性、伏笔全量兑现与结局质量。
- 对不同题材、多视角、复杂倒叙的系统性模型评测。
- 独立人工文学评审；程序完成状态不得替代这一层结论。
